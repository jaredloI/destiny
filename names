local v = {
	'4867521';
};
function w()
	for _, x in pairs(game:GetService('Players'):GetChildren()) do
		if table.find(v, (tostring(x.UserId))) then
			if x.Character then
				x.Character:FindFirstChildWhichIsA('Humanoid').DisplayName = ('[⭐] ' .. x.DisplayName)
			end;
		end;
	end;
end;
local _, y = pcall(w);
